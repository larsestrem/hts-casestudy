PASTE THE TEXT BELOW (between the lines) AS YOUR FIRST MESSAGE IN THE NEW SESSION.
Attach hts-casestudy-repo.zip to that same message if the repo isn't populated yet.
----------------------------------------------------------------------------------

You are taking over an existing project: a static, zero-JavaScript portfolio case study I
(Lars Estrem) built for the leadership of Helicopter Transport Services (HTS). It lives in
this repository, which is connected to Cloudflare Pages for automatic deployment.

Do these orientation steps before anything else, then report back:

1. If this repo does NOT already contain a `portfolio/` folder and a `build/` folder, I've
   attached `hts-casestudy-repo.zip` with the full project. Locate it (try `ls` and
   `ls /tmp`), unzip it into the repo root so the files sit at top level (`portfolio/`,
   `build/`, `flatten.py`, `deploy.sh`, `CLAUDE.md`, `HANDOFF.md`), delete the zip, then
   commit and push to `main`.
2. Read `CLAUDE.md` and `HANDOFF.md` in full. They define the architecture, the hard
   content rules (zero JS, no em dashes, measured/verifiable claims, noindex, respect HTS's
   existing sales team), the 12-page structure with its six "growth lever" pages, and how
   deployment works. Follow them exactly.
3. Run `python3 flatten.py` and confirm it prints `stray absolute refs: none` and
   `build files: 15`. Tell me the result.

How we work after that:
- I'll ask for content or design changes. You edit the SOURCE in `portfolio/`. Never
  hand-edit `build/` (it's generated by `flatten.py`).
- When I say "deploy", "ship it", or "push it live", run `./deploy.sh "<short message>"`.
  It re-runs flatten, commits `portfolio/` + `build/`, and pushes to `main`; Cloudflare then
  deploys automatically. Always confirm the flatten verification was clean before pushing.
- Don't add JavaScript, web fonts, or frameworks. Don't remove `noindex`. Don't open pull
  requests or extra branches — work toward `main`.

Start with the three orientation steps and report back, then wait for my first change
request.
