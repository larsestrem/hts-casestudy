# HTS Growth Strategy - Case Study (single-folder build)

A static, zero-JavaScript case study for Helicopter Transport Services. Every file lives in
this one folder, no subfolders, so it uploads as a simple set of files. Open `index.html`
to start. Every page is `noindex`. Replace `hero-skycrane.png` with your Skycrane image,
keeping the same filename.
